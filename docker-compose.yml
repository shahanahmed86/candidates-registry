services:    
  db:
    image: mongo:7.0.13-rc0-jammy
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongosh localhost:27017/test --quiet
      interval: 10s
      timeout: 10s
      retries: 5
      start_period: 40s
    networks:
      - app
    logging:
      options:
        max-size: 512m
